# URL base da API — embutida no bundle JavaScript em build time (NEXT_PUBLIC_*)
# Dev local (npm run dev):     http://localhost:3000
# Docker local:                http://host.docker.internal:3000  (via build arg no docker-compose)
# Produção:                    https://api.pelada-draft.com.br   (via build arg no CI/CD)
NEXT_PUBLIC_API_URL=http://localhost:3000
